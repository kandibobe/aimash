"""Константы аудита: пороги проверок + модель health-score. Всё считает КОД (golden rule #4/#6).

Health-score — ЛИНЕЙНАЯ модель с потолком штрафа НА СЕМЬЮ проверок: `penalty(family) =
FAMILY_WEIGHT[family] × min(1, Σ_findings SEVERITY_MULT[sev] × intensity)`, `score = 100 − Σ penalty`.
FAMILY_WEIGHT суммируются в 100 (полный провал всех семей → 0). Ранняя экспоненциальная зарисовка
(100·exp(−Σ/K)) заменена этой: она объяснима клиенту и ПИНУЕТСЯ golden-fixture тестом
(tests/test_audit_engine.py) — правка веса сразу ловится тестом, а не молча сдвигает всем grade (крит.C1).

Пороги — В ВАЛЮТЕ АККАУНТА (FX не выдумываем, golden rule #4); переопределяемы per-chat
(как advisor DEFAULT_ADVISOR_THRESHOLDS / UserSettings.alert_thresholds).
"""

from __future__ import annotations

from adcopy.validate import MIN_KEYWORD_COVERAGE

# Макс. штраф score НА СЕМЬЮ проверок (Σ = 100). Семьи без реализованных проверок дают 0 штрафа.
# Heartbeat (2026-07): добавлена семья delivery=8 (дизапрувы + 0-показов — тихая потеря без расхода).
# Экспертное расширение (2026-07-09): гео стало ДЕНЕЖНОЙ семьёй (geo_no_conv/schedule_waste), rsa/
# structure принимают Quality Score / SKAG / RSA-покрытие / brand-isolation. Ре-баланс профинансирован
# из bidding (broad_unmanaged — слабейшая evidence) и пустого assets (задел без проверок → честный 0):
# geo 2→8, rsa 6→7, structure 4→5; доноры delivery 8→6, bidding 8→4, assets 2→0. Σ доноров −8 = Σ
# получателей +8. waste/conversion_tracking/keywords НЕ тронуты → golden-баллы A–F не сдвигаются. Смена
# вектора ротирует SCORE_MODEL_VERSION (веса в хэше) → тренд честно «н/д» через версию (N1.0a).
#
# Ф8 (2026-07-14): семьи assets (расширения, Ф6.1) и pmax (Performance Max, Ф7) ОЖИВЛЕНЫ — их чеки
# работали, но балл не двигали. Доноры: waste 30→28, conversion_tracking 20→18, budget 10→9, geo 8→7
# (−6 = +3 assets +3 pmax). Веса малые ОСОЗНАННО: обе семьи — конфигурация (info-находки), а не
# сожжённые деньги; аккаунт БЕЗ расширений/PMax находок не получает (GR8) и не штрафуется вовсе.
# Семьи competition/recommendations остаются вне вектора (вес 0) — это диагноз и мнение Google, а не
# дефект аккаунта. Смена вектора ротирует SCORE_MODEL_VERSION → тренд честно «н/д» через версию.
FAMILY_WEIGHT: dict[str, float] = {
    "waste": 28.0,
    "conversion_tracking": 18.0,
    "keywords": 10.0,
    "budget": 9.0,
    "geo": 7.0,
    "rsa": 7.0,
    "delivery": 6.0,
    "structure": 5.0,
    "bidding": 4.0,
    "assets": 3.0,
    "pmax": 3.0,
}

# Множитель важности находки. warning доминирует, info лишь модулирует.
# Ф8: уровень critical (1.5) — для находок, которые ломают ФУНДАМЕНТ, а не отдельную кампанию:
# нет трекинга / 0 конверсий при живом трекинге (все остальные числа аудита тогда — гадание) и
# 3×-Kill (жжём втрое дороже СВОЕЙ же цели). Даёт две вещи: (1) сортировка находок — critical всегда
# первым (findings.sort по -SEVERITY_MULT), (2) неденежная critical-находка кусает score вдвое
# сильнее warning. Дальше расширять НЕ спешить: если critical у половины чеков — уровня нет.
SEVERITY_MULT: dict[str, float] = {"critical": 1.5, "warning": 1.0, "info": 0.4}

# Интенсивность НЕденежной находки (у денежной intensity = clamp(at_risk / spend)).
# 0.5 → две warning-находки в семье насыщают её потолок (min(1, Σ)); одна = половина штрафа.
NONMONEY_INTENSITY: float = 0.5

# Границы буквенной оценки (score 0..100). Пустой аккаунт → score None, grade "—" (не фейковые 100).
GRADE_BANDS: tuple[tuple[float, str], ...] = (
    (90.0, "A"),
    (80.0, "B"),
    (65.0, "C"),
    (50.0, "D"),
    (0.0, "F"),
)

# Пороги проверок (валюта аккаунта; переопределяемы per-chat). Зеркалят advisor, где пересекается.
DEFAULT_AUDIT_THRESHOLDS: dict[str, float] = {
    "min_spend": 1.0,  # шумовой порог расхода — ниже не флагуем
    "pause_min_spend": 5.0,  # ENABLED-кампания: расход при 0 конверсий → «впустую»
    "high_cpa_factor": 2.0,  # CPA кампании ≥ factor × средний по аккаунту → «дорогая конверсия»
    "kill_cpa_factor": 3.0,  # «3× Kill Rule»: CPA ≥ factor × ЦЕЛЬ (только при заданном target_cpa)
    "budget_share_pct": 60.0,  # доля расхода одной кампании, при которой смотрим эффективность
    "kw_min_spend": 3.0,  # ключ с расходом ≥ порога при 0 конверсий → кандидат в минус-слова
    "kw_top_n": 5,  # не больше N ключей за раз (анти-спам; самые дорогие)
    "min_impressions": 200.0,  # объявление: показов ≥ порога для оценки CTR
    "low_ctr_factor": 0.5,  # CTR < factor × средний по аккаунту → освежить тексты
    "single_campaign_min_spend": 10.0,  # единственная ENABLED-кампания с расходом ≥ порога
    "no_conv_min_spend": 10.0,  # аккаунт: расход есть, конверсий 0 → подозрение на трекинг
    "is_lost_min": 0.10,  # потеря impression-share ПО БЮДЖЕТУ ≥ 10% → флаг (North Country/Hassanelsisi)
    "is_rank_lost_min": 0.20,  # C: потеря IS ПО РАНГУ ≥ 20% → флаг (ранг чинится качеством, не бюджетом)
    "is_search_floor": 0.05,  # C: пол search_is для оценки упущенного (иначе impr/search_is → взрыв)
    "is_data_tolerance": 0.02,  # |Σ долей − 1.0| ≤ tol → данные полны (иначе proto3-zero «нет данных»)
    "broad_min_spend": 5.0,  # N1.2: BROAD-ключи без Smart Bidding — флаг от этого расхода на кампанию
    "kw_per_group_max": 20,  # D: >N активных ключей в группе → «свалка» (G03: ≤10 норма, >20 fail)
    "rsa_min_per_group": 2,  # D: <N активных RSA в группе → тонкое покрытие (North Country ≥2-3)
    "qs_fail": 4,  # B: Quality Score ≤ N (при cost ≥ kw_min_spend) → низкий (claude-ads G20: ≤4 fail)
    "qs_component_below_pct": 0.35,  # B: доля ключей с компонентом QS «ниже среднего» ≥ N → флаг (G22-24)
    "smart_bid_min_conv": 30,  # bidding: ручная стратегия при > N конв/период → Smart Bidding лучше (G40)
    "geo_min_spend": 20.0,  # A: регион с расходом ≥ N и 0 конверсий → слив гео (North Country)
    "schedule_min_spend": 20.0,  # A: час×день с расходом ≥ N и 0 конверсий → слив по расписанию
    "bid_gap_min": 0.10,  # Ф1: разрыв ставка↔оценка Google < N → шум оценки, не совет
    "kw_rank_lost_top_min": 0.30,  # Ф1: ключ теряет ≥ N верхних показов по РАНГУ → ставка/качество
    "sim_min_conv_gain": 0.5,  # Ф1: прирост по симулятору Google < N конверсий → шум прогноза, не совет
    # Ф3 (тексты объявлений). Порог покрытия ключей — ЕДИНЫЙ с генерацией (adcopy.validate), иначе
    # аудит требует одного, а генератор выдаёт другое: MIN_KEYWORD_COVERAGE, не своё число.
    "rsa_kw_coverage_min": MIN_KEYWORD_COVERAGE,  # G35: доля заголовков с ключом < N → релевантность
    "rsa_min_headlines": 8,  # G27: заголовков < N (из 15 возможных) → Google нечего комбинировать
    "rsa_min_descriptions": 3,  # G28: описаний < N (из 4)
    "rsa_stale_days": 90,  # G-AD1: в группе нет объявления моложе N дней → тексты выгорели
    # Ф4 (майнинг поисковых запросов). Минус-слово необратимо режет трафик — пороги консервативны.
    "harvest_min_conv": 1.0,  # G-KW3: запрос принёс ≥ N конверсий, а ключа под него нет → «в плюс»
    "harvest_top_n": 5,  # не больше N кандидатов за раз (анти-спам, как kw_top_n)
    "ngram_min_cost": 10.0,  # G16: слово жжёт ≥ N (валюта аккаунта) во всех запросах и не конвертит
    "ngram_min_terms": 2,  # …и встречается минимум в N РАЗНЫХ запросах (система, а не случай)
    "ngram_top_n": 5,
    "zero_impr_share": 0.5,  # G-KW1: доля активных ключей БЕЗ показов ≥ N …
    "zero_impr_min_count": 20,  # … И их не меньше N штук (на 8 ключах доля — не сигнал)
    # Ф5а (конкурентное давление). Карточка-диагноз по аукциону; молчит, пока давление не доказано.
    "comp_min_impressions": 500.0,  # аукцион на сотне показов — не рынок, картина недостоверна
    "comp_is_max": 0.60,  # доля показов (взвеш. по кампаниям) ниже N …
    "comp_rank_min": 0.20,  # … И потеря по РАНГУ ≥ N → тебя обходят в аукционе
    # Ф6 (денежные чеки каталога claude-ads).
    "tcpa_gap_factor": 2.0,  # G37: факт. CPA ≥ N × цель (цель < 50% факта) → кампания задушена …
    "tcpa_min_conv": 3.0,  # … и это НЕ шум одной случайной конверсии: конверсий не меньше N
    "brand_mix_min_nonbrand": 3,  # G05: в кампании с брендом ≥ N небрендовых ключей с показами …
    "brand_mix_min_spend": 20.0,  # … и расход кампании ≥ N (делить кампанию-«копейку» незачем)
    "assets_min_sitelinks": 4,  # G50: <N ситилинков на кампанию (Google даёт до 20)
    "assets_min_callouts": 4,  # G51: <N уточнений (до 20)
    "assets_min_spend": 20.0,  # расширения на кампании-«копейке» — не находка, а шум
    "content_on_search_min_spend": 5.0,  # G12: расход ПО СЕТИ КМС внутри Search-кампании ≥ N …
    "geo_interest_min_spend": 20.0,  # G11: кампания с расходом ≥ N и гео «присутствие ИЛИ интерес»
    # Ф7 (Performance Max). Поисковые запросы PMax судим ТЕМИ ЖЕ порогами, что и обычные
    # (kw_min_spend/kw_top_n): канал другой — деньги те же.
    "pmax_min_spend": 20.0,  # группа активов / кампания PMax с расходом ≥ N (ниже — шум, не находка)
    "pmax_brand_min_conv": 3.0,  # G-PM3: брендовых конверсий не меньше N (одна — случайность) …
    "pmax_brand_conv_share": 0.30,  # … и их доля ≥ N от всех конверсий кампании → PMax ест бренд
    "pmax_learn_min_conv": 30.0,  # G-PM7: PMax не обучится меньше чем на N конверсиях за 30 дней …
    "pmax_min_days": 14,  # … судим об этом только на периоде ≥ N дней (экстраполяция недели — шум)
}


# N1.0a (ревью 2026-07-08): ручная эпоха score-модели. Веса/пороги/реестр проверок хэшируются
# автоматически (audit.engine.compute_score_model_version), но СЕМАНТИКУ тела проверки (формулу
# at_risk, условия срабатывания) хэш не видит. Правишь смысл существующего чека БЕЗ смены его
# check_id/family/severity/порогов → БАМПНИ эпоху, иначе тренд покажет ложную дельту клиенту.
# Эпоха 2 (2026-07-09): экспертное расширение — новый механизм score_intensity (упущенная выгода IS
# влияет на балл при at_risk=0) + семантика усиленных чеков (high_cpa 2.0→1.5× и т.п.) хэшу не видна.
# Эпоха 3 (2026-07-13, Ф0): анти-ложноположительные правила claude-ads. Тела трёх чеков сменили
# смысл БЕЗ правки check_id/family/severity/порогов ⇒ хэш их НЕ видит (проверено: версия осталась
# e92402b21117), а находок стало объективно меньше — без бампа тренд сравнил бы несравнимое:
#   • broad_unmanaged  — legacy BMM («+ключ») больше не флажится; добавлена ветка «BROAD под Smart
#     Bidding без единого минус-слова» (G17, вторая половина — раньше не ловилась вовсе);
#   • no_negative_list — минусы ПРЯМО на кампании считаются гигиеной наравне со shared-списком (G15);
#   • adgroup_bloat    — ключи считаются по показам за период и дедупятся по тексту (G03).
# Эпоха 4 (2026-07-13, аудит D5): wasteful_search_term наследует spend_segment РОДИТЕЛЬСКОГО ключа
# («kw::кампания::ключ» вместо своего «st::…»). Расход запроса ⊂ расход его ключа, поэтому раньше
# _dedup_at_risk складывал их и «Под риском» задваивалось; теперь по сегменту берётся МАКСИМУМ.
# Ни check_id, ни family/severity, ни пороги не менялись ⇒ хэш этого не видит — нужен бамп, иначе
# тренд сравнил бы старые (завышенные) деньги-под-риском с новыми (честными).
# Ф8 (2026-07-14) эпоху НЕ бампает — и это осознанно, а не забыто. Всё, что изменила Ф8, хэш ВИДИТ
# сам: FAMILY_WEIGHT (assets/pmax ожили), SEVERITY_MULT (уровень critical), CHECK_REGISTRY (severity
# трёх чеков). Версия ротируется от них ⇒ тренд и так честно «н/д». Эпоха существует РОВНО для
# невидимого хэшу (тело проверки сменило смысл при тех же check_id/family/severity/порогах) —
# бампать её «за компанию» = приучать себя жать кнопку, не проверив, нужна ли она.
# Эпоха 5 (2026-07-14, ревизия волны): geo_interest_waste перестал класть деньги в «Под риском»
# (at_risk=0 + score_intensity = доля расхода вне таргета). Те же деньги уже несёт geo_no_conv
# (сегменты «geo::кампания::регион» vs «кампания» не пересекались ⇒ _dedup_at_risk их СКЛАДЫВАЛ, и
# «Под риском» задваивался на кампаниях с гео «присутствие ИЛИ интерес»). Ровно как в эпохе 4: тело
# чека сменило смысл при неизменных check_id/family/severity/порогах ⇒ хэш слеп — нужен бамп, иначе
# тренд сравнил бы старые (задвоенные) деньги с новыми (честными).
# Эпоха 6 (2026-07-14, ревизия багов): три чека на инвентаре ключей (zero_impression_keywords,
# keyword_cannibalization, brand_nonbrand_mixed) теперь молчат при усечённом инвентаре
# (keyword_inventory_truncated) — как harvest/ngram. На аккаунте, где инвентарь упёрся в LIMIT,
# zero_impression_keywords раньше штрафовал балл (info×NONMONEY_INTENSITY) по произвольному обрезку;
# теперь нет. check_id/family/severity/пороги те же ⇒ хэш слеп — бамп, иначе тренд сравнил бы балл,
# посчитанный по неполному списку, с честным.
SCORE_MODEL_EPOCH: int = 6


def grade_for(score: float | None) -> str:
    """Буква по score (0..100). None → «—» (нет активности)."""
    if score is None:
        return "—"
    for floor, letter in GRADE_BANDS:
        if score >= floor:
            return letter
    return "F"
