"""Keyword research (Фаза 3, БЛОК E, ТЗ §13): подбор идей + AI-кластеризация по интенту.

Имя пакета — `keywords` (множественное), чтобы не затенять stdlib-модуль `keyword`
(аналогично `adcopy` вместо `copy`). Подбор идей — в `ads.keyword_plan` (SDK), здесь —
LLM-кластеризация (`cluster`) и табличный экспорт (`export`). Всё advisory, READ-ONLY.
"""
