"""`hermes_ops` — read-only MCP-окно в дашборд Hermes (наблюдение ЗА Hermes из Claude Code).

Не путать с `mcp_server/`: тот выставляет инструменты Google Ads **для** Hermes (Hermes — клиент);
этот выставляет состояние Hermes **для** Claude Code (Hermes — наблюдаемый). Направления
противоположны, общего кода нет, кроме редакции секретов и гардов из `core/`.

Живёт на машине разработчика и ходит на дашборд по tailnet — на VPS не добавляется ни один процесс
(машина 3.7 Gi с историей oom-kill, см. `deploy/hermes/OPERATIONS.md` §14.1).

⚠️ Почему это не «удобный клиент к REST». API дашборда — 241 эндпоинт, среди которых
`POST /api/fs/write-text` (запись любого файла под root), `GET /api/fs/read-text` (чтение
`/opt/aimash/.env` с OAuth-токенами) и `POST /api/env/reveal` (выдача секретов открытым текстом).
Выставить это агенту целиком — нарушение И7 (таинт внешнего контента: в контекст агента приезжает
текст, которому доверять нельзя). Поэтому поверхность закрыта так, как требует правило 8: опасное
**не регистрируется вовсе**, а не запрещается списком.

Собственная аутентификация дашборда этот allow-list НЕ заменяет и наоборот. Замер 2026-07-29: без
сессии REST отдаёт 401 на всём, кроме короткого публичного списка (`/api/status`, `/api/model/info`,
`/api/config/defaults|schema`, `/api/dashboard/themes|plugins`, `/api/cron/fire`) — подробности и
опровержение прежней записи «дашборд без пароля» в `deploy/hermes/OPERATIONS.md` §14. Пока провайдер
входа не зарегистрирован (`auth_providers: []`), живыми остаются `hermes_status` и
`hermes_model_info`; остальные инструменты возвращают честный error-конверт с 401, а не молчат.
"""
